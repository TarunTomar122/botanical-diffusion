model:
  sample_size: 64
  in_channels: 3
  out_channels: 3
  layers_per_block: 1
  block_out_channels: [64, 128, 128, 256]
  down_block_types: ["DownBlock2D", "DownBlock2D", "AttnDownBlock2D", "DownBlock2D"]
  up_block_types: ["UpBlock2D", "AttnUpBlock2D", "UpBlock2D", "UpBlock2D"]
  dropout: 0.1
diffusion:
  num_train_timesteps: 1000
  beta_schedule: "linear"
  prediction_type: "epsilon"
training:
  resolution: 64
  center_crop: true
  random_flip: true
  train_batch_size: 4
  gradient_accumulation_steps: 1
  num_epochs: 100
  learning_rate: 1.0e-4
  lr_warmup_steps: 10
  lr_scheduler: "cosine"
  adam_beta1: 0.95
  adam_beta2: 0.999
  adam_weight_decay: 1.0e-6
  use_ema: false
  mixed_precision: "no"
  enable_xformers: false
  dataloader_num_workers: 0
  checkpointing_steps: 1000
  save_images_steps: 50
  max_train_steps: 100
  seed: 42
data:
  train_data_dir: "data/processed/curtis-64"
output:
  output_dir: "experiments/exp000_sanity_100"
